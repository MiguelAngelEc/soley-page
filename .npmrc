# Registro aprobado para este proyecto (AUD-007). La configuracion global de
# esta maquina apunta a registry.npmmirror.com; aqui se fija el registro oficial
# para que build local y CI resuelvan el mismo arbol desde el mismo origen.
registry=https://registry.npmjs.org/
